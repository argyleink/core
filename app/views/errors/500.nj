{% extends "layouts/default.nj" %}

	{% block content %}

	<section class="content">

		<h1 class="bad">{{title}}</h1>

		<pre>
			<span class="code">{{error}}</span>
		</pre>

	</section>

	{% endblock %}
